<!DOCTYPE html>
<html>
  <head>
    <title>Unit 2 Assessment</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>To Do List</h1>
    <% if(todo.length == 0) { %>
      <h3>There are no To Dos yet!</h3>
    <% } else { %>
      <% let j = 0 %>
      <ul>
        <% todo.forEach(i => { %>
          <li>
            <%= i.todo %> 
            - 
            <%= i.done ? 'Done' : 'Not Done' %>
            <form action="/delete/<%=j%>" method="POST">
              <button type="submit">DELETE</button>
            </form>
          </li>
          <% j++ %>
        <% }) %>
      </ul>
    <% } %>
    <hr>
    <form action="/new" method="POST">
      <input type="text" required name="todo"> <button type="submit">ADD TO DO</button>
    </form>
  </body>
</html>
